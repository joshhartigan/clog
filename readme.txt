clog

clog is a game with very few characters and 128 possible textures (ASCII).
it is inspired by dwarf fortress and has no objective as of yet.

it is written in C and depends upon the curses library (you probably have it)

to compile it:
  `mkdir bin`
  `make`
